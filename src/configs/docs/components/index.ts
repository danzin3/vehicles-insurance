import { Client } from './client/client.comp';
import { ErrorDefaultResponse } from './errors/errorDefault.comp';

export const components = {
  schemas: {
    Client,
    ErrorDefaultResponse,
  },
};
